{
  "Servers": {
    "1": {
      "Name": "TenantLayer demo (orders)",
      "Group": "Servers",
      "Host": "localhost",
      "Port": 55433,
      "MaintenanceDB": "orders",
      "Username": "admin",
      "SSLMode": "prefer",
      "Comment": "Persistent demo DB for the TenantLayer order-service. Connect as admin to browse; admin is a SUPERUSER and therefore bypasses row-level security, so you will see every tenant's rows."
    }
  }
}
